<html>
  <head>
    <title>Homepage</title>
    <link rel="stylesheet" href="/style.css" />
  </head>

  <body>
    <header><%- include('navbar', {links:links}) %></header>
    
    <ul>
      <% users.forEach((user) => { %> 
        <%- include('users/user', {user:user}); %>
      <%}); %>
    </ul>
  </body>
</html>
